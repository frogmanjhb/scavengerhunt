[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run setup && npm run db:generate && npm run build"

[deploy]
startCommand = "cd server && npx prisma migrate deploy && NODE_ENV=production node dist/index.js"
restartPolicyType = "ON_FAILURE"
